{{ form_start(form) }}
{{ form_rest(form) }}

{% if resource.id is not null %}
    <button class="btn btn-primary">Edit {{ resourceName }}</button>
{% else %}
    <button class="btn btn-success">Add {{ resourceName }}</button>
{% endif %}
{{ form_end(form) }}